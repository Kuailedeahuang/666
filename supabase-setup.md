# Supabase 数据库配置指南

## 1. 创建Supabase项目

1. 访问 [Supabase官网](https://supabase.com) 并注册账号
2. 创建新项目，选择合适的地域
3. 获取项目URL和anon key，填入.env文件

## 2. 数据库表结构

在Supabase SQL编辑器中执行以下SQL创建诗词表：

```sql
-- 创建诗词表
CREATE TABLE poems (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  title TEXT NOT NULL,
  author TEXT NOT NULL,
  dynasty TEXT NOT NULL,
  content TEXT NOT NULL,
  tags TEXT[] DEFAULT '{}',
  created_at TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc'::text, NOW()) NOT NULL,
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc'::text, NOW()) NOT NULL
);

-- 启用行级安全策略
ALTER TABLE poems ENABLE ROW LEVEL SECURITY;

-- 创建允许所有人读取的策略
CREATE POLICY "允许所有人读取诗词" ON poems
  FOR SELECT USING (true);

-- 创建允许认证用户插入的策略
CREATE POLICY "允许认证用户添加诗词" ON poems
  FOR INSERT WITH CHECK (auth.role() = 'authenticated');

-- 创建允许认证用户更新的策略
CREATE POLICY "允许认证用户更新诗词" ON poems
  FOR UPDATE USING (auth.role() = 'authenticated');

-- 创建允许认证用户删除的策略
CREATE POLICY "允许认证用户删除诗词" ON poems
  FOR DELETE USING (auth.role() = 'authenticated');

-- 创建索引优化搜索性能
CREATE INDEX idx_poems_title ON poems USING gin (title gin_trgm_ops);
CREATE INDEX idx_poems_author ON poems USING gin (author gin_trgm_ops);
CREATE INDEX idx_poems_content ON poems USING gin (content gin_trgm_ops);
CREATE INDEX idx_poems_tags ON poems USING gin (tags);
CREATE INDEX idx_poems_dynasty ON poems (dynasty);
```

## 3. 环境变量配置

复制 `.env.example` 为 `.env` 并填入实际值：

```env
VITE_SUPABASE_URL=你的Supabase项目URL
VITE_SUPABASE_ANON_KEY=你的Supabase匿名密钥
```

## 4. 测试配置

启动开发服务器测试连接：

```bash
npm run dev
```

## 5. 功能特性

- ✅ 诗词数据的CRUD操作
- ✅ 全文搜索（标题、作者、内容、标签）
- ✅ 按朝代分类过滤
- ✅ 用户认证支持
- ✅ 错误处理和降级方案